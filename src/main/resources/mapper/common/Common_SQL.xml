<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="common">
	<!-- top 불러오기 -->
	<select id="listByTop" resultType="hashmap" parameterType="hashmap">
	<![CDATA[
		SELECT * FROM (SELECT * FROM GOODS WHERE GOODS_CATEGORY1 = 'TOP' AND GOODS_ONOFF = '0') WHERE ROWNUM < 7
	]]>
	</select>
		<!-- outer 불러오기 -->
	<select id="listByOut" resultType="hashmap" parameterType="hashmap">
	<![CDATA[
		SELECT * FROM (SELECT * FROM GOODS WHERE GOODS_CATEGORY1 = 'OUTER' AND GOODS_ONOFF = '0') WHERE ROWNUM < 7
	]]>
	</select>
		<!-- PANTS 불러오기 -->
	<select id="listByPan" resultType="hashmap" parameterType="hashmap">
	<![CDATA[
		SELECT * FROM (SELECT * FROM GOODS WHERE GOODS_CATEGORY1 = 'PANTS' AND GOODS_ONOFF = '0') WHERE ROWNUM < 7
	]]>
	</select>
		<!-- ACC 불러오기 -->
	<select id="listByAcc" resultType="hashmap" parameterType="hashmap">
	<![CDATA[
		SELECT * FROM (SELECT * FROM GOODS WHERE GOODS_CATEGORY1 = 'ACC' AND GOODS_ONOFF = '0') WHERE ROWNUM < 7
	]]>
	</select>
		<!-- SHOES 불러오기 -->
	<select id="listByShoes" resultType="hashmap" parameterType="hashmap">
	<![CDATA[
		SELECT * FROM (SELECT * FROM GOODS WHERE GOODS_CATEGORY1 = 'SHOES' AND GOODS_ONOFF = '0') WHERE ROWNUM < 7
	]]>
	</select>
</mapper>